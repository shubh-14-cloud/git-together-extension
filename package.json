{
  "name": "git-together-tracker",
  "repository": {
    "type": "git",
    "url": "https://github.com/shubh-14-cloud/git-together-extension"
  },
  "publisher": "shubhdev",
  "displayName": "git-together-tracker",
  "description": "Tracks coding activity and shares it with friends.",
  "version": "0.0.7",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "git-together.setUsername",
        "title": "Git Together: Set Username"
      },
      {
        "command": "git-together.joinRoom",
        "title": "Git Together: Join Room"
      },
      {
        "command": "git-together.refresh",
        "title": "Git Together: Refresh",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "git-together.refresh",
          "when": "view == git-together-sidebar",
          "group": "navigation"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "git-together-sidebar",
          "title": "Git Together",
          "icon": "$(broadcast)"
        }
      ]
    },
    "views": {
      "git-together-sidebar": [
        {
          "type": "webview",
          "id": "git-together-sidebar",
          "name": "Active Friends"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.8",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
